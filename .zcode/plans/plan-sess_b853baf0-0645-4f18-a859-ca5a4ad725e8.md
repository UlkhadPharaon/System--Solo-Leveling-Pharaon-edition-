# Plan — Intégration des assets « Ka Rise » (chantier visuel/UX uniquement)

## Constats clés de l'exploration

- App React 19 + Vite + Tailwind 4, PWA web uniquement (pas de dossier android/ios natif — les « icônes Android adaptatives » se branchent donc dans `manifest.json` en icônes `maskable`/`monochrome`).
- Les 16 « onglets » du cahier des charges = 8 onglets principaux (`Header.tsx:61-70`) + 9 sous-onglets du hub SYSTÈME (`SystemSoloLeveling.tsx:726-736` : Statut, Missions, Donjons, Armée Divine, Forge Royale, Boutique, Classement, Journal, Custom).
- Progression de rang dans le code (`lib/utils.ts:266-281`) : E(1+) → D(5) → C(7) → B(10) → A(15) → S(20) → Pharaon(25) — l'ordre logique est correct, aucun changement métier nécessaire.
- Barres HP/MP/XP actuelles : CSS pur (`SystemSoloLeveling.tsx:680-721`), badges de rang = badges texte CSS (`getRankBadgeStyle`, lignes 358-377).
- Branding obsolète : « SYSTEM » (`Header.tsx:85`), titre « S Y S T E M | Pharaoh » (`index.html`), « Pharaoh System » (`manifest.json`), cache `pharaoh-system-v2` (`sw.js`), `icon.jpg` partout (favicon, apple-touch, notification).
- `showSystemIntro` existe (`App.tsx:111-123`) mais n'est **jamais rendu** → aucun splash visuel actuellement ; parfait pour y brancher l'écran de démarrage fourni.
- Aucune meta Open Graph actuellement.

## Étape 1 — Normalisation des assets

Copier les fichiers de `UI element and references/` (noms avec fautes) vers `src/assets/ui/` avec noms normalisés (les originaux restent intacts) :
- `badge-rank-E/D/C/B/A/S.png`, `badge-rank-pharaon.png` (corrige « badge ranc C », « badge rand D »)
- `icon-statut.png`, `icon-missions.png`, `icon-donjons.png`, `icon-armee-ombre.png`, `icon-forge.png`, `icon-classement.png`
- `bar-xp.png`, `bar-hp.png`, `bar-mp.png`, `bar-hpmp.png`
- `quest-card-frame.png` (2048×768, format large — usable en `border-image` ou fond de carte)
- `soldier-assassin.png`, `soldier-wolf.png`, `mount-dragon.png`
- `logo-full.png`, `logo-icon.png`, `splash.png`, `og-share.png`, `android-adaptive.png`, `notification-mono.png`, `badge-set.png`
- Copier vers `public/` : `favicon.png`, `og-share.png`, `logo-icon.png`, icônes maskable/monochrome (servies par URL brute).

## Étape 2 — Module central d'assets `src/lib/uiAssets.ts` + composants

- Mapping rang→image (E→D→C→B→A→S→Pharaon, ordre logique explicite) + ordre de progression pour affichage.
- `<RankBadge rank size>` : remplace `getRankBadgeStyle` partout (hub header, cartes donjon `SystemSoloLeveling.tsx:1026`, soldats ombre `:1133`), avec micro-glow pulsé discret (`.anim-glow` existant) sur le rang courant du joueur.
- `<ResourceBar type xp|hp|mp|hpmp>` : fond = asset fourni (9-slice via `border-image` ou img de fond), remplissage = div gradient animé (transition fluide, pas de saut). HPMP combiné pour le header compact du hub ; XP/HP/MP séparés là où l'espace le permet (Statut).
- Cadre de quête : appliqué aux cartes de missions quotidiennes, donjons et quêtes narratives (`NarrativeQuestsView`) en fond avec padding/typographie ajustés pour respirent avec les ornements — pas juste plaqué.

## Étape 3 — Icônes de section & illustrations

- Sous-onglets du hub (`SystemSoloLeveling.tsx:726-736`) : remplacer les icônes lucide par les 6 icônes fournies (Statut, mission, donjons, armée de l'ombre, forge, classements) ; lucide conservé pour Journal/Custom/Boutique (pas d'asset fourni).
- Header principal : logo icône à la place du `Zap` cyan (`Header.tsx:80-82`).
- **Décision (à noter dans le résumé §6)** : assassin/loup/dragon = illustrations vedettes dans l'onglet Armée Divine — galerie « Légendes de l'Ombre » (unités d'exception à afficher / débloquer), dragon aussi mis en avant dans la Forge comme monture royale. Usage purement visuel, zéro logique métier.
- `badge-set.png` : inspecté à l'implémentation ; s'il s'agit d'une strip des 7 rangs → utilisée comme frise de progression dans Statut (E→Pharaon avec rangs verrouillés en grisé/opacity).

## Étape 4 — Branding « Ka Rise » + config build réelle

- `Header.tsx:85` : « SYSTEM » → « KA RISE » + logo ; chaînes « SYSTÈME » user-facing ajustées si cohérent (sans toucher aux libellés fonctionnels du hub).
- `index.html` : titre « Ka Rise — Système du Pharaon », favicon → `/favicon.png`, apple-touch-icon → `/logo-icon.png`, meta OG/Twitter complètes avec `/og-share.png`.
- `public/manifest.json` : name/short_name « Ka Rise », icônes PNG 512 (any) + `android-adaptive.png` (maskable) + `notification-mono.png` (monochrome). L'asset Android est un **fichier unique** → utilisé tel quel en maskable (pas de pair fg/bg fourni).
- `public/sw.js` : bump cache `karise-v3` pour invalider l'ancien shell.
- Icône de notification (`App.tsx:504` `icon: '/icon.jpg'`) → icône monochrome.
- Splash : rendre enfin `showSystemIntro` (`App.tsx`) comme overlay plein écran avec `splash.png`, logo et fondu (une seule fois par session de chargement).

## Étape 5 — Micro-animations (esprit HUD retenu)

- Glow pulsé sur badge de rang actif (réutilise `glowPulse`), remplissage fluide des barres (transition cubic-bezier déjà en place dans `index.css:192-194`), apparition douce des cartes encadrées (`.anim-pop`), `prefers-reduced-motion` respecté (déjà en place). Pas de palette modifiée.

## Étape 6 — Vérification

- `npm run lint` (tsc) + `npm run build`, rebuild de `dist/`.
- Parcours manuel : compléter une quête quotidienne (réclamation XP), monter de niveau, vérifier passage de rang (badge change E→D etc.), ouverture Armée Divine, achat boutique. Aucune modification de `lib/utils.ts` (logique), `dailyEngine`, syncEngine.

## Non couvert / signalé dans le résumé final

- Décisions ambiguës tranchées : usage assassin/loup/dragon, usage de `badge-set.png`, barre combinée vs séparées, icône Android en fichier unique maskable.
- Incohérences assets constatées : noms de fichiers avec fautes (« ranc », « rand », « armeer », « notificatio »), à lister précisément avec résolutions après inspection complète à l'implémentation.