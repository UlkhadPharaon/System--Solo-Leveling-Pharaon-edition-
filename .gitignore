node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example
# Web Push subscription storage (contains device push endpoint — treat as a secret)
# Root-anchored so it does NOT ignore src/data/ (tracked source files)
/data/
